# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
*.log

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Config files with sensitive data - DO NOT COMMIT PRODUCTION CONFIGS
config.json
config-server-internal.json
config-*.json
*-config.json
# Only config-example.json should be in the repo

# Build artifacts
scp-go-identity-service
scp-go-identity-service.exe
# Legacy names (keep for backwards compatibility during migration)
jjp-identity-service
jjp-identity-service.exe

# Docker
.dockerignore

# Deployment folder (generated by build.bat)
deploy/

# Temporary files
*.tmp
*.temp